/* const initialState = {
  products: [],
}

export const r = (state = initialState, action) => {
  switch (action.payload) {
    case 'ADD_PRODUCTS':
      return {
        ...state,
        action,
      }
    case 'REMOVE_PRODUCTS':
      return {
        ...state,
        action,
      }
    default:
      return state
  }
}
 */
